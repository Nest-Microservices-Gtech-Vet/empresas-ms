// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Empresa {
  emp_id        Int    @id @default(autoincrement())
  emp_nombre    String @db.VarChar(255)
  emp_correo    String @unique @db.VarChar(255)
  emp_direccion String @db.VarChar(255)
  emp_telefono  String @db.VarChar(10)
  emp_ruc       String @unique @db.VarChar(13)
  emp_tipo_empresa String @db.VarChar(255)
  emp_foto  String? @db.VarChar(255)


  provincia_id Int?
  provincia    Provincia? @relation(fields: [provincia_id], references: [prov_id])

  canton_id Int?
  canton    Canton? @relation(fields: [canton_id], references: [can_id])



  //usua_admin_id Int // ID del usuario (está en otro microservicio)
  empresaUsuario EmpresaUsuario[]

  createdBy Int? // ID del superadmin (está en otro microservicio)
  updatedBy Int?

  activo         Boolean  @default(true)
  fecha_inicio DateTime @default(now())
  fecha_fin    DateTime

  created_at     DateTime @default(now())
  updated_at     DateTime @updatedAt

}

model Provincia {
  prov_id     Int       @id @default(autoincrement())
  prov_nombre String    @db.VarChar(50)
  activo      Boolean   @default(true)
  created_at  DateTime  @default(now())
  updated_at  DateTime  @updatedAt
  cantones    Canton[]
  empresas    Empresa[]

  createdBy Int?
  updatedBy Int?
}

model Canton {
  can_id       Int       @id @default(autoincrement())
  can_nombre   String    @db.VarChar(255)
  provincia_id Int
  provincia    Provincia @relation(fields: [provincia_id], references: [prov_id])
  activo       Boolean   @default(true)
  created_at   DateTime  @default(now())
  updated_at   DateTime  @updatedAt
  empresas     Empresa[]

  createdBy Int? // ID del superadmin (está en otro microservicio)
  updatedBy Int?
}

model EmpresaUsuario {
  id         Int      @id @default(autoincrement())
  empresaId  Int
  usuarioId  Int
  empresa    Empresa  @relation(fields: [empresaId], references: [emp_id])
  created_at DateTime @default(now())
}


